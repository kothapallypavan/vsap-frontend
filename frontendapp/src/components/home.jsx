import { Button } from "react-bootstrap";

const Home = () =>{
    return (
      <div>
        <Button variant="primary">Hello World</Button>
        
      </div>
      );
    
};
export default Home;